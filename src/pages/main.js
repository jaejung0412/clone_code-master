import React from "react";
import Header from "../Shared/header";
import Post from "../components/Post";

const Main = (props) => {
  return (
    <React.Fragment>
      <Header />
      <Post />
    </React.Fragment>
  );
};

export default Main;
